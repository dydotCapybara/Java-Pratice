1.public
访问权限：对所有类可见。
应用场景：通常用于类、方法和变量，需要在任何地方都能访问时使用。

2.protected
访问权限：对同一包内的类和所有子类（包括不同包中的子类）可见。
应用场景：用于需要对子类开放访问权限，但不希望其他非子类类访问的成员。

3.default（无修饰符）
访问权限：仅对同一包内的类可见。
应用场景：用于只需要在同一个包内访问的成员。

4.private
访问权限：仅对当前类可见。
应用场景：用于不希望其他类访问的成员，通常用于封装类的内部实现细节。